// 21_block_dependence.cvl
rule noBlockTimestampDependence(method f) {
    env e1; env e2;
    require e1.block.timestamp != e2.block.timestamp;
    calldataarg args;
    f(e1, args);
    f(e2, args);
    assert lastStorage == currentStorage, "Outcome depends on block timestamp";
}
